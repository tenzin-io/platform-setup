my_cluster:
  children:
    bootstrap_node:
      hosts:
       nvidia-1:
          skip_phase_mark_control_plane: True
    control_plane:
      hosts:
    worker_nodes:
      hosts:
  vars:
    control_plane_endpoint_name: dev-k8s
    ansible_ssh_user: root