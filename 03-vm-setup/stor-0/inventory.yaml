fileservers:
  children:
    storage_nodes:
      hosts:
        stor-0.vm.vhost-1:
  vars:
    # ansible
    ansible_ssh_user: tenzin-bot
    ansible_ssh_private_key_file: tenzin-bot.key
    ansible_become: True